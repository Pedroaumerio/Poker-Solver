/**
 * HOME PAGE
 * 
 * Página de seleção do formato de jogo.
 * Re-exporta o componente HomeScreen para manter a separação
 * entre page (roteamento) e componente (visual).
 */

export { default } from '../../features/home/HomeScreen';
